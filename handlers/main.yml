---
# handlers/main.yml — metrics-aggregation role handlers

- name: Reload systemd user daemon
  ansible.builtin.systemd:
    daemon_reload: true
    scope: user
  become: true
  become_user: "{{ influxdb3_service_user }}"
  listen: "influxdb3 daemon-reload"
